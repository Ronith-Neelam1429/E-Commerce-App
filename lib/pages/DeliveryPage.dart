import 'package:flutter/material.dart';

class Delivarypage extends StatelessWidget {
  const Delivarypage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Delivery in Progress"),
      ),
    );
  }
}
